% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_outlier_distributions.R
\name{plot_outlier_distribution}
\alias{plot_outlier_distribution}
\title{Create a histogram with percentile vertical lines}
\usage{
plot_outlier_distribution(data, metric, percentiles = c(1, 2, 90, 95, 99))
}
\arguments{
\item{data}{A dataframe containing the metric of interest.}

\item{metric}{A character string specifying the column name of the metric to plot.}

\item{percentiles}{A numeric vector of percentiles to display as vertical lines (default: c(1, 2, 90, 95, 99)).}
}
\value{
A ggplot object.
}
\description{
This function creates a histogram of a specified metric from a given dataframe and adds vertical lines at the specified percentiles. It supports optional faceting by a categorical variable.
}
\examples{
\dontrun{
# Create example data
example_data <- data.frame(id = 1:100,
                           height = rnorm(100, mean = 10, sd = 2),
                           species = factor(rep(c("A", "B", "C", "D"),
                            each = 25)))
# Plot the histogram
plot <- plot_outlier_distribution(example_data, metric = "height",
percentiles = c(1, 2, 90, 95, 99))
print(plot)
}
}
